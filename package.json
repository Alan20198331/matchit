{
  "private": true,
  "name": "matchit",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "pnpm --parallel run -r watch",
    "clean": "pnpm run -r clean",
    "clean:hard": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "build": "pnpm run generate:dockerfile && pnpm run -r build && echo 'SUCCESS!'"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "4.9.5"
  },
  "resolutions": {}
}
